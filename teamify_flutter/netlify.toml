[build]
  # Replace YOUR-BACKEND.onrender.com with your actual Render service URL
  # after deploying the Flask backend.
  command = "flutter build web --dart-define=API_BASE_URL=https://teamify-backend-5hq0.onrender.com"
  publish = "build/web"

[build.environment]
  FLUTTER_VERSION = "3.41.7"
  GIT_SUBMODULE_STRATEGY = "none"

[[redirects]]
  from = "/*"
  to   = "/index.html"
  status = 200
